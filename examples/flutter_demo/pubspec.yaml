name: flutter_demo
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.6.0
  flutter: ^3.27.0
resolution: workspace

dependencies:
  flutter:
    sdk: flutter

  inject_annotation: ^1.0.0-alpha.5
  inject_flutter: ^1.0.0

dev_dependencies:
  build_runner: ^2.4.15
  flutter_lints: ^5.0.0
  inject_generator: ^1.0.0-alpha.8
  test: ^1.24.9

flutter:
  uses-material-design: true
