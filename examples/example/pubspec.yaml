name: example
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.6.0
  flutter: ^3.27.0
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  inject_annotation: ^2.0.0-pre1
  inject_flutter: ^2.0.0-pre1


dev_dependencies:
  build_runner: ^2.4.15
  flutter_lints: ^5.0.0
  inject_generator: ^2.0.0-pre1

flutter:
  uses-material-design: true
